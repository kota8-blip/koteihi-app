# 🔥 実装トレーニング課題一覧

---

## 🚀 **課題5：カートのCRUD操作 + ローディング表示（NEW）**

### 📋 課題内容

**`pages/cart.vue`に以下の機能を実装せよ：**

1. **カート商品の追加（Create）**
   - 商品追加ボタンを押すと、mockサーバーにPOSTリクエスト
   - `POST http://localhost:3001/cartItems`
   - レスポンスを受け取ったら、Vuexの`state.cartItems`を更新

2. **カート商品の更新（Update）**
   - 数量を変更したら、mockサーバーにPUTリクエスト
   - `PUT http://localhost:3001/cartItems/:id`
   - レスポンスを受け取ったら、Vuexの`state.cartItems`を更新

3. **カート商品の削除（Delete）**
   - 削除ボタンを押すと、mockサーバーにDELETEリクエスト
   - `DELETE http://localhost:3001/cartItems/:id`
   - レスポンスを受け取ったら、Vuexの`state.cartItems`から削除

4. **ローディング表示**
   - API通信中は「読み込み中...」を表示
   - ボタンを`disabled`にして連打防止

5. **エラーハンドリング**
   - API通信に失敗したら、エラーメッセージを表示
   - 「通信エラーが発生しました。もう一度お試しください。」

---

### 🎯 この課題で習得できるスキル

- ✅ **POST/PUT/DELETEリクエスト**（バックエンド寄り）
- ✅ **json-serverへのCRUD操作**
- ✅ **ローディング状態の管理**
- ✅ **エラーハンドリング**
- ✅ **Vuexとの連携**（stateの更新）
- ✅ **UI/UX改善**（ローディング表示、連打防止）

---

### 💡 ヒント

#### POST/PUT/DELETEの書き方

```javascript
// POST
import { request } from "~/assets/utils/request";
async addToCart(item) {
  this.isLoading = true;
  try {
    const res = await request.post('/cartItems', item);
    this.$store.commit('cart/SetCartItems', [...this.$store.state.cart.cartItems, res]);
  } catch (error) {
    this.errorMessage = '通信エラーが発生しました。';
  } finally {
    this.isLoading = false;
  }
}

// PUT
async updateCart(id, quantity) {
  this.isLoading = true;
  try {
    const res = await request.put(`/cartItems/${id}`, { quantity });
    const updated = this.$store.state.cart.cartItems.map(item =>
      item.id === id ? res : item
    );
    this.$store.commit('cart/SetCartItems', updated);
  } catch (error) {
    this.errorMessage = '通信エラーが発生しました。';
  } finally {
    this.isLoading = false;
  }
}

// DELETE
async deleteCart(id) {
  this.isLoading = true;
  try {
    await request.delete(`/cartItems/${id}`);
    this.$store.dispatch('cart/deleteItem', { id });
  } catch (error) {
    this.errorMessage = '通信エラーが発生しました。';
  } finally {
    this.isLoading = false;
  }
}
```

---

### ⚡ 難易度
**★★★★☆（上級）** - バックエンド連携 + 状態管理 + エラーハンドリング

---

### 🔥 チャレンジ目標
**POST/PUT/DELETEを実装して、バックエンド連携を完璧にしよう！**

---

---

## 🚀 **課題4：注文履歴ページの実装（完了✅）**

### 📋 課題内容

**`pages/order.vue`に以下の機能を実装せよ：**

1. **注文履歴一覧を表示する**
   - ユーザーごとの過去の注文を一覧表示
   - 注文日、店舗名、合計金額、注文した商品数を表示

2. **注文詳細を表示する**
   - 各注文をクリックすると、注文詳細（商品名、個数、価格）をモーダルまたは別ページで表示

3. **注文履歴の検索・フィルタリング**
   - 店舗名で検索できる機能を追加
   - 日付順（新しい順・古い順）で並び替えできる機能を追加

4. **注文の再注文機能**
   - 「再注文」ボタンを押すと、その注文内容がカートに追加される

5. **空の状態の表示**
   - 注文履歴がない場合、「注文履歴はありません」というメッセージを表示

---

### 🎯 この課題で習得できるスキル

- ✅ **API通信でデータ取得**
- ✅ **Vuexまたはローカルデータでの状態管理**
- ✅ **コンポーネント設計**（一覧、詳細、検索フォーム）
- ✅ **検索・フィルタリング機能**
- ✅ **並び替え機能**（ソート）
- ✅ **モーダル表示**
- ✅ **条件付きレンダリング**（空の状態）

---

### ⚡ 難易度
**★★★★★（上級）** - 複数の機能を組み合わせた実践的な課題

---

### 🔥 チャレンジ目標
**自分で設計・実装してみよう！**

---

---

## 🚀 **課題3：フォームバリデーション機能（完了✅）**

### 📋 課題内容

**`pages/edit.vue`に以下のバリデーション機能を追加せよ：**

1. **名前のバリデーション**
   - 空欄チェック（必須入力）
   - 2文字以上
   - エラーメッセージ：「名前は2文字以上で入力してください」

2. **電話番号のバリデーション**
   - 空欄チェック（必須入力）
   - 数字のみ
   - 10〜11桁
   - エラーメッセージ：「電話番号は10〜11桁の数字で入力してください」

3. **メールアドレスのバリデーション**
   - 空欄チェック（必須入力）
   - `@`を含む形式チェック
   - エラーメッセージ：「正しいメールアドレスを入力してください」

4. **エラー表示**
   - 各入力フォームの下に赤文字でエラーメッセージを表示
   - バリデーションエラーがある場合、保存ボタンを無効化

5. **保存ボタンの制御**
   - 全てのバリデーションが通った場合のみ保存可能
   - エラーがある場合はボタンを`disabled`にする

---

### 🎯 この課題で習得できるスキル

- ✅ **フォームバリデーション**（入力チェック）
- ✅ **正規表現**（数字チェック、メール形式チェック）
- ✅ **computed**（複数条件の判定）
- ✅ **条件付きレンダリング**（エラーメッセージ表示）
- ✅ **ボタンの無効化制御**（disabled属性）

---

### 💡 ヒント

#### バリデーション用のcomputed

```javascript
computed: {
  ...mapGetters("userInfo", ["userInfo"]),
  
  // 名前のエラーメッセージ
  nameError() {
    if (!this.userProfile.name) {
      return '名前を入力してください';
    }
    if (this.userProfile.name.length < 2) {
      return '名前は2文字以上で入力してください';
    }
    return '';
  },
  
  // 電話番号のエラーメッセージ
  mobileError() {
    if (!this.userProfile.mobile) {
      return '電話番号を入力してください';
    }
    if (!/^\d{10,11}$/.test(this.userProfile.mobile)) {
      return '電話番号は10〜11桁の数字で入力してください';
    }
    return '';
  },
  
  // メールアドレスのエラーメッセージ
  emailError() {
    if (!this.userProfile.email) {
      return 'メールアドレスを入力してください';
    }
    if (!this.userProfile.email.includes('@')) {
      return '正しいメールアドレスを入力してください';
    }
    return '';
  },
  
  // 全体のバリデーション結果
  isValid() {
    return !this.nameError && !this.mobileError && !this.emailError;
  }
}
```

#### テンプレートでの使用例

```vue
<div class="userName_edit">
  <input
    v-model="userProfile.name"
    type="text"
    placeholder="名前を入力してください"
  >
  <p v-if="nameError" class="error">{{ nameError }}</p>
  <button
    v-if="userProfile.name !== originalName.name"
    :disabled="!isValid"
    @click="saveProfileName(userProfile.name)"
  >
    保存
  </button>
</div>
```

#### CSSでエラー表示

```css
.error {
  color: #ff0000;
  font-size: 12px;
  margin-top: 4px;
}

button:disabled {
  background: #ccc;
  cursor: not-allowed;
  opacity: 0.6;
}
```

---

### ⚡ 難易度
**★★★★☆（中級〜上級）** - 実務で必須のバリデーション機能

---

### 🔥 チャレンジ目標
**30分以内に実装完了** - さらなる爆速成長！

---

---

## 📝 **今後の課題候補（優先度順）**

### 🔥 優先度：高（次にやるべき）

#### **課題6：ページネーション機能**
- 難易度：★★★☆☆
- 習得スキル：ページング、配列の分割表示、UI/UX
- 内容：注文履歴やショップリストを10件ずつ表示し、ページ送りボタンで切り替え

#### **課題7：無限スクロール機能**
- 難易度：★★★★☆
- 習得スキル：スクロールイベント、遅延ロード、パフォーマンス最適化
- 内容：スクロールが下部に達したら自動的に次のデータを読み込む

---

### ⚡ 優先度：中（実務でよく使う）

#### **課題8：トースト通知機能**
- 難易度：★★☆☆☆
- 習得スキル：グローバルコンポーネント、アニメーション、タイマー制御
- 内容：「カートに追加しました」「保存しました」などの通知を画面上部に表示

#### **課題9：localStorageで永続化**
- 難易度：★★★☆☆
- 習得スキル：ブラウザストレージ、データの永続化、JSON変換
- 内容：カート内容やログイン状態をlocalStorageに保存し、ページリロード後も復元

#### **課題10：画像アップロード機能**
- 難易度：★★★★☆
- 習得スキル：File API、FormData、画像プレビュー、バックエンド連携
- 内容：ユーザーのプロフィール画像をアップロードして保存

---

### 🚀 優先度：やや低（さらなるスキルアップ）

#### **課題11：認証機能（ログイン/ログアウト）**
- 難易度：★★★★★
- 習得スキル：JWT、Cookie/Session、認証状態管理、ルートガード
- 内容：ログイン画面を作成し、認証トークンを管理してページアクセスを制御

#### **課題12：リアルタイム検索（デバウンス）**
- 難易度：★★★☆☆
- 習得スキル：デバウンス、パフォーマンス最適化、API通信の制御
- 内容：検索ワード入力中、一定時間入力がなかったらAPI通信を実行

---

### 💡 長期目標（起業に向けて）

#### **課題13：決済機能（Stripe連携）**
- 難易度：★★★★★
- 習得スキル：決済API、セキュリティ、バックエンド連携
- 内容：クレジットカード決済機能を実装

#### **課題14：リアルタイムチャット機能**
- 難易度：★★★★★
- 習得スキル：WebSocket、リアルタイム通信、バックエンド連携
- 内容：ユーザー同士がリアルタイムでメッセージを送受信

---

**次の実装トレーニングは「課題5：カートのCRUD操作 + ローディング表示」を推奨！**  
**バックエンド連携の基本を完璧にして、実務レベルのスキルを習得しよう！**

---

---

## 🚀 **課題2：ユーザー情報編集ページ（完了✅）**
📋 課題内容
pages/profile/edit.vueを新規作成し、以下の機能を実装せよ：

ログイン中ユーザーの情報を表示

名前、電話番号、メールアドレスを入力フォームで表示
login APIからデータ取得（user_idでフィルタ）
入力フォームで情報を編集可能にする

v-modelで双方向バインディング
名前、電話番号、メールアドレスを編集可能
保存ボタン

クリックでalertで「保存しました」を表示（実際のPUT処理は不要）
データが変更されていることをコンソールで確認
キャンセルボタンクリックで元のデータに戻す

🎯 この課題で習得できるスキル
✅ v-model（フォーム入力の双方向バインディング）
✅ オブジェクトのコピー（元データの保持）
✅ API連携（user_idでフィルタリング）
✅ データのリセット処理
✅ 条件分岐（編集前後の状態管理）

💡 ヒント

// ログインユーザーのデータ取得
async initData() {
  let res = await login(this.userInfo.user_id);
  if (res && res.length > 0) {
    this.userProfile = { ...res[0] }; // コピーして保持
    this.originalProfile = { ...res[0] }; // 元データ保存
  }
}

// キャンセル処理
cancel() {
  this.userProfile = { ...this.originalProfile };
}


---

---

## 🚀 **課題1：商品カート機能（完了✅）**

### 📋 課題内容

**`pages/cart.vue`を新規作成し、以下の機能を実装：**

1. **カート内の商品リストを表示**
   - 商品名、価格、数量を表示
   - モックデータから取得

2. **商品の数量変更機能**
   - `+`ボタンで数量を1増やす
   - `-`ボタンで数量を1減らす（最小値1）

3. **合計金額の自動計算**
   - `computed`を使って、全商品の合計金額をリアルタイム計算
   - 数量変更時に自動更新

4. **削除ボタン**
   - 各商品に削除ボタンを配置
   - クリックで商品をカートから削除

---

### 🎯 習得したスキル

- ✅ **配列操作**（splice、mapの活用）
- ✅ **computed**（複雑な計算処理）
- ✅ **メソッド実装**（増減・削除処理）
- ✅ **データバインディング**（双方向データフロー）
- ✅ **条件分岐**（最小値制御）

---
