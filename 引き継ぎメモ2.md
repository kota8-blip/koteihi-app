# 習慣トラッカーアプリ - 開発引き継ぎメモ

## アプリ概要
Nuxt 2 (SPA mode) で作った習慣トラッカーアプリ。
毎日のルーティンを管理し、達成率をカレンダーで確認できる。

---

## 技術スタック
- Nuxt 2 (SPA mode)
- Vuex (状態管理)
- v-calendar v2.4.2 (カレンダー表示)
- vuedraggable (ドラッグ&ドロップ)
- localStorage (データ永続化)

---

## ファイル構造
```
pages/
  index.vue          ← メイン画面（ルーティン一覧）
  settings.vue       ← 設定画面
  history/
    index.vue        ← カレンダー履歴画面
    _date.vue        ← 過去日付のスナップショット画面（動的ルート）
components/
  routineListBox.vue
  completedRoutineListBox.vue
  baseFooter.vue
  baseHeader.vue
  colorModal.vue
  cheatDayModal.vue
  achievementRateModal.vue
  svg.vue
store/
  index.js
layouts/
  default.vue
```

---

## Vuex Store (store/index.js)

### state
```javascript
{
  routineList: [],           // 未完了ルーティン
  completedRoutineList: [],  // 完了済みルーティン
  currentColor: '#a0bad8',   // テーマカラー
  cheatDay: [],              // チートデイの曜日配列（weekday番号）
  achievementRate: null      // 達成率目標（数値 or null）
}
```

### localStorage キー
| キー | 内容 |
|------|------|
| routineList | ルーティン一覧 |
| completedRoutineList | 完了済みリスト |
| lastResetDate | 最終リセット日 |
| dailyRates | 日別データ（後述） |
| currentColor | テーマカラー |
| cheatDay | チートデイ曜日配列 |
| achievementRate | 達成率目標 |

### dailyRates の構造
```javascript
{
  "2026-03-20": {
    rate: 80,
    routineList: [...],
    completedRoutineList: [...]
  }
}
```
index.vue の `watch: { rating }` でリアルタイム保存される。

### 主なミューテーション
- `LOAD_FROM_STORAGE` : 起動時にlocalStorageから全データ復元 + 日付チェックで日次リセット
- `SAVE_TO_STORAGE` : 全データをlocalStorageに保存
- `SET_CURRENT_COLOR` : テーマカラー変更
- `SET_CHEAT_DAY` : チートデイのトグル（indexOf/push/spliceで配列管理）
- `SET_ACHIEVEMENT_RATE` : 達成率目標をセット（単純な値代入。配列ではない）

---

## 各ページの説明

### pages/index.vue
- routineListBox と completedRoutineListBox を表示
- `rating` computed で達成率を計算
- `watch: { rating }` で変更のたびにdailyRatesをlocalStorageに保存（スナップショット含む）

### pages/history/index.vue
- v-calendar で月カレンダーを表示
- `v-slot:day-content="{ day }"` で各日のセルをカスタマイズ
- v-calendarが各日付の情報を `day` オブジェクトとして渡す（day.year, day.month, day.day, day.weekday）
- `getDayStyle(day)` で背景色を決定：
  - チートデイ → 灰色
  - achievementRate設定あり → 目標以上=緑、未満=赤
  - achievementRate未設定 → 80%以上=緑、50%以上=オレンジ、0%超=赤
- 日付クリックで `/history/YYYY-MM-DD` に遷移（データがある日のみ）

### pages/history/_date.vue
- Nuxt動的ルート。`_date` という名前が `params.date` のキーになる
- URLの `this.$route.params.date` で日付文字列（例: "2026-03-20"）を取得
- localStorageのdailyRatesからスナップショットを読み込み表示
- 表示専用
- data: `snapshotRoutineList`, `snapshotCompletedRoutineList`, `snapshotRate`, `date`

### pages/settings.vue
- colorModal / cheatDayModal / achievementRateModal をボタンクリックで開閉

---

## 各コンポーネントの説明

### colorModal.vue
- パステルカラー7色を表示（Red/Green/Blue/Yellow/Purple/White/Black）
- colors配列: `{ name, code, class }`
- 選択中の色に✔表示（右端）、`:class="color.class"` で行背景色を変更
- `SET_CURRENT_COLOR` をcommit

### cheatDayModal.vue
- 月〜日の曜日を表示
- days配列: `{ name, weekday }` （weekday: 1=月〜7=日）
- `SET_CHEAT_DAY` でトグル（選択済みなら解除）

### achievementRateModal.vue
- 0〜100%（10刻み）を表示
- 選択中に灰色背景 + ✔表示
- `SET_ACHIEVEMENT_RATE` で単純セット

### baseFooter.vue
- Home / 履歴 / 設定 / 更新 の4タブ
- 更新タブは `window.location.reload()`

---

## layouts/default.vue
- `:style="{backgroundColor: currentColor, minHeight: '100vh'}"` で全画面背景色
- html要素の背景色もwatch+mountedでcurrentColorに同期（DevTools開閉での色漏れ防止）
- mounted で `LOAD_FROM_STORAGE` 実行

---

## 解決済みバグ・注意点

### CSSの注意
- `<style scoped>` はSCSSネスト非対応。ネストは使わずフラットに書く
- 2つのクラスを同一要素に当てる場合: `class="常時クラス" :class="{ 条件クラス: 条件 }"` の形で書く
- `height: 60vh` のコンテナに `height: 150%` の子要素を入れるとフッターを突き抜ける → `height: 100%` + `overflow: hidden` で対応

### Nuxtルーティングの注意
- `pages/history.vue` と `pages/history/_date.vue` を共存させるとネストルートになりページ遷移が機能しない
- `pages/history/index.vue` に変更することで独立したページになる

### Vuexの注意
- `SET_ACHIEVEMENT_RATE` はトグル配列ではなく単純な値代入（`state.achievementRate = payload`）
- stateの初期値は `achievementRate: null`（`[]` にするとindexOfクラッシュする）
- `LOAD_FROM_STORAGE` で cheatDay と achievementRate も必ず復元すること

### その他
- completedRoutineListBoxの削除ボタンに `@click.stop` が必要（イベント伝播防止）
- dailyRatesに旧形式（数値のみ）と新形式（オブジェクト）が混在する可能性があるため、`getRate()` で `typeof entry === 'object' ? entry.rate : entry` の互換処理が必要

---

## 現在の状態
- 全機能実装済み・リファクタリング完了
- 次は新しいアプリ作成フェーズへ移行予定
