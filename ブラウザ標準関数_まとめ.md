# ブラウザ標準関数まとめ

ブラウザが提供する超頻出の標準関数・API集

## 📌 重要ポイント
- **インポート不要**: これらの関数はブラウザに組み込まれているため、`import`や`require`が不要
- **グローバルオブジェクト**: `window`オブジェクトのメソッドとして提供される
- **ブラウザ専用**: Node.jsなどのサーバー環境では使えない（一部例外あり）

---

## 🔍 ブラウザ標準関数を見抜く方法

### **存在しない関数を探し続けるのを防ぐ判別パターン**

#### パターン1: `window.` で始まる → 100%ブラウザAPI
```javascript
window.alert()
window.confirm()
window.setTimeout()
window.location
window.localStorage
```
**→ プロジェクト内を探すな。MDNを見ろ。**

#### パターン2: `document.` で始まる → 100%ブラウザDOM API
```javascript
document.querySelector()
document.getElementById()
document.addEventListener()
```
**→ プロジェクト内を探すな。MDNを見ろ。**

#### パターン3: `console.` で始まる → 標準API
```javascript
console.log()
console.error()
console.table()
```
**→ プロジェクト内を探すな。**

#### パターン4: グローバル関数（超頻出のみ）
```javascript
setTimeout()  // window.setTimeout の省略形
setInterval()
fetch()
alert()  // window.alert の省略形
```
**→ これらはブラウザ標準。ただしカスタム関数の可能性もあるので注意。**

#### パターン5: 組み込みオブジェクトのメソッド
```javascript
JSON.stringify()
JSON.parse()
Math.random()
Date.now()
Object.keys()
Array.isArray()
```
**→ JavaScript言語仕様。プロジェクト内を探すな。**

---

### **実践的な判別フロー**

```
関数を見つけた
    ↓
Q1: `import` や `require` されてる？
    YES → プロジェクト内の関数 → 探せ
    NO → Q2へ
    ↓
Q2: `window.` `document.` `console.` で始まる？
    YES → ブラウザ標準 → 探すな
    NO → Q3へ
    ↓
Q3: グローバルスコープで呼ばれてる？（例: setTimeout, fetch, alert）
    YES → たぶんブラウザ標準 → MDNで検索
    NO → Q4へ
    ↓
Q4: `this.メソッド名()` の形？（Vueコンポーネント内）
    YES → methods内を探せ
    NO → Q5へ
    ↓
Q5: プロジェクト内でgrep検索
    見つかった → そこに定義がある
    見つからない → ブラウザ標準の可能性大
```

---

### **VSCodeで即座に確認する方法**

1. **関数名にカーソルを合わせる**
2. **F12（定義へジャンプ）を押す**
3. **結果を判定**:
   - TypeScript型定義ファイル（`.d.ts`）に飛んだ → **ブラウザ標準またはライブラリ標準**
   - プロジェクト内のファイルに飛んだ → **プロジェクト内の関数**
   - 何も起きない → **grep検索するか、ブラウザ標準を疑う**

---

### **ブラウザ開発者ツールで確認する方法**

```javascript
// コンソールで実行
console.log(typeof window.alert);  // "function" → ブラウザ標準
console.log(typeof myCustomFunc);  // "undefined" → 自作関数（まだ読み込まれてない）
```

---

### **迷ったらMDNで検索**

1. **Google検索**: `"関数名 mdn"` で検索
2. **結果がMDN（Mozilla Developer Network）に出た** → ブラウザ標準
3. **結果が出ない** → プロジェクト内の関数を探せ

**例**: 
- `"querySelector mdn"` → MDNに出る → ブラウザ標準
- `"myCustomFunction mdn"` → 出ない → プロジェクト内を探せ

---

### **暗記すべき超頻出ブラウザ標準（これだけで9割カバー）**

```javascript
// ダイアログ
alert(), confirm(), prompt()

// タイマー
setTimeout(), setInterval(), clearTimeout(), clearInterval()

// DOM操作
document.querySelector(), document.querySelectorAll()
document.getElementById(), document.createElement()
element.addEventListener(), element.classList

// コンソール
console.log(), console.error(), console.warn(), console.table()

// ストレージ
localStorage, sessionStorage

// 通信
fetch()

// ウィンドウ
window.location, window.open(), window.scrollTo()
window.innerWidth, window.innerHeight

// JSON・Math・Date
JSON.stringify(), JSON.parse()
Math.random(), Math.floor(), Math.ceil(), Math.round()
Date.now(), new Date()

// 配列・オブジェクト
Array.isArray(), Object.keys(), Object.values(), Object.entries()
```

**これらを見たら反射的に「ブラウザ標準だ」と判断できるようになれ。**

---

## 1. ダイアログ系

### `window.alert()`
**用途**: ユーザーへのメッセージ表示（確認ボタンのみ）
```javascript
window.alert('商品をカートに追加しました');
// window. は省略可能
alert('エラーが発生しました');
```

### `window.confirm()`
**用途**: ユーザーに確認を求める（OK/キャンセル）
```javascript
const result = confirm('本当に削除しますか?');
if (result) {
  // OKが押された場合 (true)
  deleteItem();
} else {
  // キャンセルが押された場合 (false)
  console.log('キャンセルされました');
}
```

### `window.prompt()`
**用途**: ユーザーからの入力を受け取る
```javascript
const name = prompt('お名前を入力してください');
if (name) {
  console.log(`こんにちは、${name}さん`);
}
// キャンセルされた場合は null が返る
```

---

## 2. タイマー系

### `setTimeout()`
**用途**: 指定時間後に1回だけ実行
```javascript
// 3秒後に実行
setTimeout(() => {
  console.log('3秒経過しました');
}, 3000);

// キャンセルも可能
const timerId = setTimeout(() => {
  console.log('実行されない');
}, 5000);
clearTimeout(timerId); // タイマーをキャンセル
```

### `setInterval()`
**用途**: 指定時間ごとに繰り返し実行
```javascript
// 1秒ごとに実行
const intervalId = setInterval(() => {
  console.log('1秒経過');
}, 1000);

// 停止する場合
clearInterval(intervalId);
```

---

## 3. コンソール出力系

### `console.log()`
**用途**: 開発者ツールにログ出力（デバッグ用）
```javascript
console.log('通常のログ');
console.log('変数の値:', myVariable);
console.log('複数の値', value1, value2, value3);
```

### `console.error()`
**用途**: エラーメッセージを赤色で出力
```javascript
console.error('エラーが発生しました');
```

### `console.warn()`
**用途**: 警告メッセージを黄色で出力
```javascript
console.warn('この機能は非推奨です');
```

### `console.table()`
**用途**: 配列やオブジェクトを表形式で見やすく表示
```javascript
const users = [
  { name: '太郎', age: 25 },
  { name: '花子', age: 30 }
];
console.table(users);
```

---

## 4. DOM操作系

### `document.querySelector()`
**用途**: CSSセレクターで要素を1つ取得
```javascript
const button = document.querySelector('.btn-submit');
const header = document.querySelector('#header');
const firstItem = document.querySelector('li'); // 最初の<li>のみ
```

### `document.querySelectorAll()`
**用途**: CSSセレクターで要素を全て取得（NodeListが返る）
```javascript
const allButtons = document.querySelectorAll('.btn');
allButtons.forEach(btn => {
  btn.addEventListener('click', () => {
    console.log('クリックされた');
  });
});
```

### `document.getElementById()`
**用途**: IDで要素を取得（古い方法だが高速）
```javascript
const header = document.getElementById('header');
```

### `element.addEventListener()`
**用途**: 要素にイベントリスナーを追加
```javascript
const button = document.querySelector('.btn');
button.addEventListener('click', (event) => {
  console.log('ボタンがクリックされました', event);
});
```

### `element.classList`
**用途**: CSSクラスの追加・削除・トグル
```javascript
const element = document.querySelector('.box');
element.classList.add('active'); // クラス追加
element.classList.remove('hidden'); // クラス削除
element.classList.toggle('open'); // トグル（あれば削除、なければ追加）
element.classList.contains('active'); // クラスが存在するか確認
```

---

## 5. ローカルストレージ系

### `localStorage`
**用途**: ブラウザにデータを永続的に保存（文字列のみ）
```javascript
// 保存
localStorage.setItem('username', '太郎');
localStorage.setItem('settings', JSON.stringify({ theme: 'dark' }));

// 取得
const username = localStorage.getItem('username');
const settings = JSON.parse(localStorage.getItem('settings'));

// 削除
localStorage.removeItem('username');

// 全削除
localStorage.clear();
```

### `sessionStorage`
**用途**: ブラウザタブを閉じるまで一時的に保存
```javascript
sessionStorage.setItem('tempData', '一時データ');
const data = sessionStorage.getItem('tempData');
```

---

## 6. JSON操作系

### `JSON.stringify()`
**用途**: JavaScriptオブジェクトをJSON文字列に変換
```javascript
const user = { name: '太郎', age: 25 };
const jsonString = JSON.stringify(user);
// 結果: '{"name":"太郎","age":25}'

// localStorageに保存する際に必須
localStorage.setItem('user', JSON.stringify(user));
```

### `JSON.parse()`
**用途**: JSON文字列をJavaScriptオブジェクトに変換
```javascript
const jsonString = '{"name":"太郎","age":25}';
const user = JSON.parse(jsonString);
// 結果: { name: '太郎', age: 25 }

// localStorageから取得した値を使う際に必須
const savedUser = JSON.parse(localStorage.getItem('user'));
```

---

## 7. 非同期処理系

### `fetch()`
**用途**: HTTPリクエストを送信（Ajax通信）
```javascript
// GET リクエスト
fetch('https://api.example.com/users')
  .then(response => response.json())
  .then(data => {
    console.log(data);
  })
  .catch(error => {
    console.error('エラー:', error);
  });

// async/await 版
async function getUsers() {
  try {
    const response = await fetch('https://api.example.com/users');
    const data = await response.json();
    console.log(data);
  } catch (error) {
    console.error('エラー:', error);
  }
}

// POST リクエスト
fetch('https://api.example.com/users', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({ name: '太郎', age: 25 })
});
```

### `Promise`
**用途**: 非同期処理の結果を扱う
```javascript
const promise = new Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('成功しました');
  }, 1000);
});

promise
  .then(result => {
    console.log(result); // '成功しました'
  })
  .catch(error => {
    console.error(error);
  });
```

---

## 8. ウィンドウ・画面操作系

### `window.location`
**用途**: 現在のURLを取得・変更
```javascript
// 現在のURL取得
console.log(window.location.href); // https://example.com/page?id=123
console.log(window.location.pathname); // /page
console.log(window.location.search); // ?id=123

// ページ遷移
window.location.href = 'https://google.com';

// リロード
window.location.reload();
```

### `window.open()`
**用途**: 新しいウィンドウ・タブを開く
```javascript
window.open('https://google.com', '_blank'); // 新規タブで開く
```

### `window.scrollTo()`
**用途**: ページの指定位置にスクロール
```javascript
window.scrollTo(0, 0); // 一番上にスクロール
window.scrollTo({
  top: 500,
  behavior: 'smooth' // スムーズスクロール
});
```

### `window.innerWidth / window.innerHeight`
**用途**: ブラウザの表示領域のサイズ取得
```javascript
console.log(`幅: ${window.innerWidth}px`);
console.log(`高さ: ${window.innerHeight}px`);
```

---

## 9. 日付・時刻系

### `Date`
**用途**: 日付と時刻を扱う
```javascript
// 現在の日時
const now = new Date();
console.log(now); // Tue Oct 22 2025 10:30:00 GMT+0900

// 年月日時分秒を取得
console.log(now.getFullYear()); // 2025
console.log(now.getMonth()); // 9 (0始まりなので10月は9)
console.log(now.getDate()); // 22
console.log(now.getHours()); // 10
console.log(now.getMinutes()); // 30
console.log(now.getSeconds()); // 0

// タイムスタンプ取得
console.log(now.getTime()); // 1729558200000
console.log(Date.now()); // 現在のタイムスタンプ

// 特定の日時を作成
const birthday = new Date('2000-05-15');
```

---

## 10. その他の超頻出関数

### `parseInt() / parseFloat()`
**用途**: 文字列を数値に変換
```javascript
parseInt('123'); // 123
parseInt('123.45'); // 123（整数部分のみ）
parseFloat('123.45'); // 123.45
parseInt('10', 2); // 2進数として解釈 → 2
```

### `isNaN()`
**用途**: 値が数値かどうか判定
```javascript
isNaN('abc'); // true（数値ではない）
isNaN('123'); // false（数値に変換可能）
isNaN(123); // false（数値）
```

### `Math` オブジェクト
**用途**: 数学的な計算
```javascript
Math.random(); // 0以上1未満のランダムな数値
Math.floor(4.7); // 4（切り捨て）
Math.ceil(4.1); // 5（切り上げ）
Math.round(4.5); // 5（四捨五入）
Math.max(1, 5, 3); // 5（最大値）
Math.min(1, 5, 3); // 1（最小値）
Math.abs(-5); // 5（絶対値）
Math.pow(2, 3); // 8（2の3乗）
Math.sqrt(16); // 4（平方根）
```

---

## 📝 使い分けのポイント

### Vue.jsでの注意点
Vue.jsなどのフレームワークを使っている場合でも、これらのブラウザ標準関数は普通に使える。
ただし、より良い方法がフレームワーク側に用意されている場合もある。

```javascript
// ❌ あまり良くない（Vueでは）
document.querySelector('.btn').addEventListener('click', () => {});

// ✅ より良い（Vueの機能を使う）
<button @click="handleClick">クリック</button>
```

### Node.jsとの違い
- ❌ **Node.jsでは使えない**: `window`, `document`, `alert`, `localStorage` など
- ✅ **どちらでも使える**: `console.log`, `setTimeout`, `JSON.stringify`, `Date` など

---

## 🎯 実践例: よく使う組み合わせ

```javascript
// ローカルストレージに配列を保存
const cart = [{ id: 1, name: '商品A' }, { id: 2, name: '商品B' }];
localStorage.setItem('cart', JSON.stringify(cart));

// 取得して使う
const savedCart = JSON.parse(localStorage.getItem('cart')) || [];
console.table(savedCart);

// APIからデータ取得してローカルストレージに保存
async function fetchAndSave() {
  try {
    const response = await fetch('https://api.example.com/products');
    const products = await response.json();
    localStorage.setItem('products', JSON.stringify(products));
    alert('データを保存しました');
  } catch (error) {
    console.error('エラー:', error);
    alert('データの取得に失敗しました');
  }
}

// 3秒後にページトップにスムーススクロール
setTimeout(() => {
  window.scrollTo({ top: 0, behavior: 'smooth' });
}, 3000);
```

---

これらの関数は**JavaScript自体に組み込まれている**または**ブラウザが提供している**ため、どのプロジェクトでもインポートなしで使える超重要な基礎知識！
